# Triggered when a GitHub Release is published.
# The publish jobs run inside the `marketplace` environment, which gates them
# behind required reviewers and branch policy.

name: Publish the VS Code Extension

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  publish-vsce:
    name: Publish to VS Code Marketplace
    runs-on: ubuntu-latest
    environment: marketplace
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm run pretest
      - uses: azure/login@v3
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          allow-no-subscriptions: true
      - run: pnpm run publish -- --azure-credential

  publish-ovsx:
    name: Publish to Open VSX
    runs-on: ubuntu-latest
    environment: marketplace
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm run package
      - run: pnpm exec ovsx publish --pat ${{ secrets.OPEN_VSX_MARKETPLACE_TOKEN }}
